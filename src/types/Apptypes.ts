export enum AppUrls {
  CHARACTERS = "/characters",
  LOCATIONS = "/locations",
  EPISODES = "/episodes",
}

export type Label = {
  it: string;
  en: string;
};

export enum LanguageEnum {
  IT = "it",
  EN = "en",
}
